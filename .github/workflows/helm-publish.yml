name: Helm

on:
  push:
    tags: ['v*']

jobs:
  helm:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: azure/setup-helm@v4

      - name: Package chart
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          sed -i "s/^appVersion:.*/appVersion: \"$TAG\"/" charts/tinymon/Chart.yaml
          helm package charts/tinymon -d .helm-packages

      - name: Publish to gh-pages
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git checkout gh-pages 2>/dev/null || git checkout --orphan gh-pages
          git rm -rf . 2>/dev/null || true
          cp .helm-packages/*.tgz .
          helm repo index . --url https://unclesamwk.github.io/TinyMon
          git add *.tgz index.yaml
          git commit -m "Helm chart $GITHUB_REF_NAME"
          git push origin gh-pages --force
