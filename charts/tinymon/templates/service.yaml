apiVersion: v1
kind: Service
metadata:
  name: {{ include "tinymon.fullname" . }}
  labels:
    {{- include "tinymon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tinymon.selectorLabels" . | nindent 4 }}
